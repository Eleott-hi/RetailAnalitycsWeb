



version: '3'

services:
  test:
    image: python:3.11
    container_name: test
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install -r requirements.txt; python3 main.py"
